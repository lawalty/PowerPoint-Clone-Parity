{
  "name": "powerpoint-clone-parity",
  "version": "1.0.0",
  "description": "A PowerPoint clone: presentation document model, editing engine, rendering, and UI with full feature parity across core PowerPoint capabilities.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
